[package]
name = "glucose-core"
version = "1.0.0"
edition = "2021"
description = "Glucose Core Engine — Moteur géométrique, algorithmique et modèle de données pur (0 dépendance)"

[dependencies]
# AUCUNE DÉPENDANCE EXTERNE — 100% Rust Standard Library (std)
